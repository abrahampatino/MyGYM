@using System.Security.Claims;
@{string user = string.Empty;}


@if (User.Identities.Any(u => u.IsAuthenticated))
{
    user = @User.Identities.First(
    u => u.IsAuthenticated &&
    u.HasClaim(c => c.Type == ClaimTypes.Name)).FindFirst(ClaimTypes.Name).Value;
    
}
else
{
    
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Proyectov1</title>
    <link rel="stylesheet" href="~/css/Page.css" />
    <link rel="stylesheet" href="~/css/Home/Home.css" />
</head>
<body>
    <header>
        <div class="nav-content">
            <ul class="box-shadow border-bottom navbar">
                <li class="nav-tittle-item"><a class="nav-item nav-tittle" href="/">MyGYM</a></li>
                <li class="nav-item-li"><a class="nav-item" href="/">Home</a></li>
                <li class="nav-item-li"><a class="nav-item" href="/Home/AcercaDe">Acerca De</a></li>
            </ul>
        </div>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>

    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            <hr />
            <div class="container-foot">
                &copy; 2019 - MyGYM
            </div>
        </div>
    </footer>
    <script src="~/js/site.js" asp-append-version="true"></script>

    @RenderSection("Scripts", required: false)
</body>
</html>
